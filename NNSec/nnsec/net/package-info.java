/**
 * Provides the classes necessary to exploit the NNSec Protocol (or rather,
 * Neural Network Secure Protocol).
 * <p>
 * This subpackage contains classes which are useful to build a neural network
 * and to manage some neural networks correctly.
 *
 * @since 1.0
 * @author Michele Caini
 */
package nnsec.net;
